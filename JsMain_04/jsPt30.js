// SOME MORE WITH FOREACH

const coding = ['js' ,'ruby' , 'java' , 'python' , 'ts']

// What if we store it in a variable

const values = coding.forEach((item) => {
    return item          // Will be undefines so we get to know ha foreach dont return anything
})

console.log(values);
console.log();


